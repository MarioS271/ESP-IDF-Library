# Logger

This is my own implementation of the ESP-IDF logger, adjusted to fit my likings a little.

Implements:
* LOGE(tag, format, ...)
* LOGW(tag, format, ...)
* LOGI(tag, format, ...)
* LOGD(tag, format, ...)
* LOGV(tag, format, ...)
